#include <iostream>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int l, h; //divides a day into L units of time (numbered 1 through L).....body requires spending only H continuous units of time per day on sleep
        string s;
        int l = s.length();
        cin >> l >> h;
        cin >> s;
        if (/* condition */)
        {

            cout << "YES" << endl;
        }
        else
        {
            cout << "NO" << endl;
        }

        cout << e << endl;
    }
    return 0;
}